name: getsecure_dart
description: Secure links for limited time access with simple md5 hash
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crypto: ^3.0.3
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0 